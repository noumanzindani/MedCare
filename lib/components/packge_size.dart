import 'package:flutter/material.dart';

class PackgeSize extends StatefulWidget {
  const PackgeSize({super.key});

  @override
  State<PackgeSize> createState() => _PackgeSizeState();
}

class _PackgeSizeState extends State<PackgeSize> {
  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}